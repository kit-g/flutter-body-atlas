name: flutter_body_atlas
description: "A new Flutter package project."
version: 0.0.1
homepage:

environment:
  sdk: ^3.10.8
  flutter: ">=1.17.0"

dependencies:
  flutter: { sdk: flutter }
  flutter_svg: ^2.2.3
  path_drawing: ^1.0.1
  xml: ^6.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  assets:
    - assets/
    - assets/svg/

